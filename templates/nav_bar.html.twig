<nav class="navbar navbar-expand-lg navbar-dark ftco_navbar bg-dark ftco-navbar-light" id="ftco-navbar">
	    <div class="container">
	      <a class="navbar-brand" href="{{ url('home') }}">Čoko<small>Moko</small></a>
	      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#ftco-nav" aria-controls="ftco-nav" aria-expanded="false" aria-label="Toggle navigation">
	        <span class="oi oi-menu"></span> Menu
	      </button>
	      <div class="collapse navbar-collapse" id="ftco-nav">
	        <ul class="navbar-nav ml-auto">
	          <li class="nav-item {{ (url == 'home') ? 'active' : '' }}"><a href="{{ url('home') }}" class="nav-link">Početna</a></li>
	          <li class="nav-item {{ (url == 'about_us') ? 'active' : '' }}"><a href="{{ url('about_us') }}" class="nav-link">O nama</a></li>
	          <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="room.html" id="dropdown04" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Shop</a>
              <div class="dropdown-menu" aria-labelledby="dropdown04">
              	<a class="dropdown-item" href="{{ url('articles') }}">Proizvodi</a>
              </div>
            </li>
	          <li class="nav-item {{ (url == 'contact') ? 'active' : '' }}"><a href="{{ url('contact') }}" class="nav-link">Kontakt</a></li>
			  {% if (app.user != null) %}
			  {% set name = app.user.name|split(' ') %}
			  <li class="nav-item" ><a href="{{ url('app_logout') }}" class="nav-link"><small>Zdravo, {{ name[0] }} </small> (Izloguj se)</a></li>
			  {% else %}
			  <li class="nav-item {{ (url == 'login') ? 'active' : '' }}" ><a href="{{ url('login') }}" class="nav-link">Prijavite se</a></li>
			  {% endif %}
	          <li class="nav-item cart"><a href="{{ url('cart') }}" class="nav-link"><span class="icon icon-shopping_cart"></span><span class="bag d-flex justify-content-center align-items-center"><small>
			  {% if (app.user != null) %}
			  	{% for cart in cart_counter %}
				{{ cart }}
			  	{% endfor %}
			  {% else %}
				{{ 0 }}
			  {% endif %}
			  </small></span></a></li>
			  	{% if (app.user.getIsAdmin() is defined) %}
			  	 	<li class="nav-item {{ (url == 'admin') ? 'active' : '' }}"><a href="{{ url('admin') }}" class="nav-link">Admin panel</a></li>
			   {% endif %}
	        </ul>
	      </div>
		  </div>
	  </nav>